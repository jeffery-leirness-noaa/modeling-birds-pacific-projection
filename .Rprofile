source("renv/activate.R")

# Sys.setenv(AML_SAS_PROCESSING = readr::read_csv(".amlsas", show_col_types = FALSE) |>
#              dplyr::filter(container == "processing") |>
#              dplyr::pull(sas))
